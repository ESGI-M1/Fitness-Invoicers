{% extends 'base.html.twig' %}

{% block title %}Mot de passe oublié envoyé{% endblock %}

{% block body %}

    <div class="flex h-screen">
        {% include './layout/welcome_nav.html.twig' %}
        <div class="w-full lg:w-3/4 bg-no-repeat bg-cover"
             style="background-image: url('/images/templates/background-welcome.png');">
            <div class="flex min-h-full flex-col px-6 py-12 lg:px-8">
                <div class="sm:mx-auto sm:w-full sm:max-w-sm">
                    {% include './components/title.html.twig' with {
                        'balise': 'h2',
                        'title': 'Réinitialiser votre mot de passe',
                        'class' : 'mt-20 text-center text-3xl font-bold leading-9 tracking-tight text-white'
                    } %}
                </div>

                <div class="mt-10 sm:mx-auto sm:w-full sm:max-w-sm">
                    <div class="p-4 bg-gray-100 bg-opacity-80 rounded-lg">
                        <p class="mb-2">
                            Si un compte correspondant à votre adresse e-mail existe, alors un e-mail vient d'être envoyé contenant un lien que vous pouvez utiliser pour réinitialiser votre mot de passe.
                            Ce lien expire dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
                        </p>
                        <p>Si vous n'avez pas reçu l'e-mail, veuillez vérifier votre dossier de courrier indésirable ou{% include './components/a_login.html.twig' with {
                                'path': path('app_forgot_password_request'),
                                'class': 'text-sm ml-2 hover:text-primary-hover text-primary underline',
                                'label': 'Réessayer',
                            } %}
                        </p>
                    </div>

                    <div class="flex justify-end my-4">
                        {% include './components/a_login.html.twig' with {
                            'path': path('app_login'),
                            'class': 'text-sm ml-2 hover:text-primary-hover text-primary',
                            'label': 'Se connecter ?',
                        } %}
                    </div>
                    {% include './components/text_connection.html.twig' with {
                        'title': 'Pas encore membre ?',
                        'text': 'Créer un compte',
                        'link': path('app_register')
                    } %}
                    {% include './layout/footer_security.html.twig' %}
                </div>
            </div>
        </div>
    </div>
{% endblock %}

