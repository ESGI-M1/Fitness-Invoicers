<button
        type="{{ type }}"
        class="flex w-full justify-center rounded-md bg-{{ bg_color }} text-{{ text_color }} px-3 py-1.5 text-sm font-semibold leading-6 text-white shadow-sm hover:bg-{{ bg_hover_color }} focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-{{ bg_color }}"
>
    {{ label }}
</button>
